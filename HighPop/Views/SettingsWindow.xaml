<Window x:Class="HighPop.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:HighPop.Views"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Settings" Width="700" Height="600"
        MinWidth="500" MinHeight="400"
        WindowStyle="None" AllowsTransparency="False"
        ResizeMode="CanResizeWithGrip"
        Background="{StaticResource BgDeepBrush}"
        WindowStartupLocation="CenterOwner">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0" ResizeBorderThickness="7" CornerRadius="0"
                            GlassFrameThickness="0" UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>

    <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="0"
            Background="{StaticResource BgDeepBrush}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="42"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Titlebar -->
            <Border Grid.Row="0" Background="{StaticResource BgSurfaceBrush}"
                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1"
                    CornerRadius="0" MouseLeftButtonDown="TitleBar_MouseDown">
                <Grid>
                    <TextBlock Text="⚙  Settings" VerticalAlignment="Center" Margin="16,0"
                               FontSize="13" FontWeight="SemiBold"
                               Foreground="{StaticResource TextPrimaryBrush}"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,3,5,3">
                        <Button Content="—" Width="34" Style="{StaticResource GhostButton}" Padding="0"
                                Click="MinimizeClick"/>
                        <Button x:Name="MaximizeButton" Content="☐" Width="34" Margin="3,0"
                                Style="{StaticResource GhostButton}" Padding="0"
                                Click="MaximizeClick"/>
                        <Button Content="✕" Width="34" Style="{StaticResource DangerButton}" Padding="0"
                                Click="CloseClick"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Content -->
            <views:SettingsView Grid.Row="1" x:Name="SettingsViewControl"/>
        </Grid>
    </Border>
</Window>
